# Текущее состояние работы

Обновлено 23 сентября 2026. Сверять с Git; требования и задачи теперь в OpenSpec.

## Начать отсюда

- Цель и продуктовые этапы: [ROADMAP](ROADMAP.md).
- Требования, границы версий и рабочий процесс: [OpenSpec](../openspec/README.md).
- Согласовано: панель Word строго опциональна; обычная загрузка DOCX/PDF и сборка самостоятельны.

## Ветки и работа

- Текущая ветка `codex/word-workflow`; OpenSpec и независимый запуск панели зафиксированы в `c2d235e`. После этого сверены исходные договорённости, уточнены базовые сценарии и приёмка конвертации; приложение в этом уточнении не менялось. Последний реальный тест панели: Microsoft 365 Windows 16.0.17928.20148. Проверены вставка/добавление, обновление, защита старого плана, сохранение/открытие и передача DOCX.
- Пилот `v0.1.3-rc.5` / `27bb8a1` остаётся опубликованным. Его исправления не перенесены в Word-ветку. Документация main (`065517b`) также должна быть сохранена при интеграции.
- PR #2 — Word, PR #3 — исправления; состояние перед работой проверять заново. Этот шаг не объединяет PR и не выпускает релиз.
- Настроен OpenSpec 1.8.0, пять базовых областей и три активных изменения. База описывает rc.5; наличие спецификации не доказывает соответствие старой текущей ветки. Статусы перенесены без фиктивного архивирования.
- Найден и исправлен отказ всего запуска при сломанном необязательном HTTPS Word. Основной HTTP продолжает работать, сообщение доступно в разделе панели; конфигурация пользователя не меняется.

## Последние проверки

- `openspec validate --all --strict --no-interactive`: восемь объектов прошли (5 specs, 3 changes).
- 124 теста Python, 7 JavaScript и Vite build прошли на Windows при подготовке `c2d235e`. После сверки договорённостей изменены только Markdown-файлы: повторно проверены структура OpenSpec и локальные ссылки документации, тесты приложения не перезапускались.
- Независимость запуска проверена в отдельных настоящих процессах: без настройки, испорченный JSON, отсутствующий сертификат, занятый HTTPS-порт. При испорченном JSON прошли обычная загрузка DOCX/PDF, сопоставление и сборка ZIP без панели с конвертацией через реальный Word Windows; цели ссылок в DOCX/PDF указывают на включённый PDF. Конфигурация осталась неизменной.
- В браузере проверены основной интерфейс и сообщение о недоступном подключении панели; предупреждение не перекрывает работу.
- Изменения запуска на Mac не запускались; живой Office.js на Mac остаётся незакрытой задачей. Новый CI OpenSpec до push не запускался удалённо.

## Следующая работа

- Продолжать задачи `openspec/changes/consolidate-release-line/`, не начинать предложенные будущие этапы ROADMAP автоматически.
- Завершить приёмку `optional-word-authoring` и `submission-revisions-and-history`: форматирование, короткие названия, режим исправлений, новые решения по сноскам, пакетные проверки и Mac.
- В старом серверном экспорте Word-ветки всё название пока жирное: это известная интеграционная задача, не исправлена одним изменением панели.

## Локальные материалы (вне Git)

- `output/word-live-office/` — прежние искусственные DOCX/ZIP/PDF и `checks.json`; данные тестового сервера `output/word-live-office/data`. Адреса были HTTP 8777 / HTTPS 8778; наличие процесса проверять.
- `output/openspec-qa-location.json` — путь к временному изолированному проекту и отчёту новой проверки запуска.
- `output/context-before-openspec-2026-09-23.md` — сохранённая прежняя подробная передача контекста, не источник актуальных требований.
- Частный разбор прежнего дефекта изображений находится в `%TEMP%/bl-image-loss-review/`; не публиковать. Случай отсутствующей подписи не закрыт: её не было уже в переданном DOCX, нужен исходник до загрузки.
- Повторно предоставленная переписка для сверки: вложение текущей задачи `a48bdd82-bc6f-4ca5-b067-2168ecf693d2` в локальной папке вложений Codex; в Git не переносить. Подтверждения результата rc.5 в ней ещё нет.

Пользователь уже подтверждал основные ручные проверки пилота; не просить повторять их без изменения или новой причины. Для управления Word согласовать время, если пользователь работает за компьютером.
