# Текущее состояние работы

Обновлено 25 сентября 2026. Требования — в OpenSpec; сверять с Git.

## Выпуск и следующий этап

- Опубликован обычный [v0.3.0](https://github.com/Ivan-Shivarshinov/bl-exhibit-manager/releases/tag/v0.3.0): резервное копирование, восстановление/независимая копия проекта и исправление ложного конфликта оформления Word.
- PR #4 объединён в `main`, текущая рабочая ветка — `main`. Тег v0.3.0 указывает на merge `b401640`; пакеты собраны из `1be8dc2`, дерево файлов которого полностью совпадает с тегом. Последующие изменения документации отмечают факт выпуска.
- `project-backup-and-restore` завершён (16/16 задач), обе базовые спецификации синхронизированы; change в `openspec/changes/archive/2026-09-25-project-backup-and-restore/`.
- Следующие согласованные этапы: `self-service-onboarding`, затем `case-material-library`. Код этих этапов ещё не реализован.

## Проверки

- Финальный CI `36161947277`: Windows x64, Mac Intel и Apple Silicon — 186 Python + 17 JS, сборка/запуск пакетов, взаимное восстановление архивов. LibreOffice на обоих Mac проверил изображения, оглавление, ссылки и новые редакции. OpenSpec: CI `36161947786`, локальная строгая проверка 12/12.
- Пользователь 25 сентября подтвердил ручные проверки исправленной панели Word Windows: восстановленная копия → проверка ссылок → применение нового названия. Повторно не запрашивать без новой причины. Office.js на Mac этим не проверен. Прежняя подтверждённая проверка панели сохраняет свой статус.
- Контрольные суммы всех шести опубликованных assets совпадают с локальными; в трёх ZIP проверены commit сборки, новое сравнение OOXML и инструкция восстановления. Публичные assets — только приложения и SHA-256.
- Более ранние браузерные проверки, настоящий Word-конвертер и нагрузка архива 537 МБ — в tasks архивированного change. Пользователь передаёт коллегам релизы и сообщает замечания; отдельный формальный ответ не нужен.

## Локальная среда

- Пользователь попросил не мешать работе за компьютером: без нового явного разрешения не переключать окна, не запускать Word, не использовать мышь/клавиатуру. Фоновая работа разрешена.
- Выпущенные пакеты: `output/release-v030-20260925/packages/`; текст релиза, проверка состава и SHA-256 — в родительской папке. `output/release-v030/packages/` — прежние пакеты без исправления OOXML, не публиковать повторно.
- Ручная среда: `output/manual-v030-20260925/Start-test.cmd`, учебные данные в `data/`, HTTP 8765 / HTTPS 8778. Dev-панель запускается через `Open-Word-test.cmd`; причина прежнего отказа обычного открытия не установлена, полный перезапуск Word его не устранял. Проверка с работающей панелью пользователем пройдена.
- Учебные документы, OOXML и headless-проверка в `output/manual-v030-20260925/`; другие проверки — `%TEMP%/bl-backup-qa/`, `output/backup-office/`. Частные материалы `%TEMP%/bl-image-loss-review/` и остальные пользовательские файлы не коммитить и не публиковать.
