# Текущее состояние работы

Обновлено 25 сентября 2026. Требования и задачи — в OpenSpec; сверять с Git.

## Текущий этап

- Ветка `codex/project-backup-and-restore`, [PR #4](https://github.com/Ivan-Shivarshinov/bl-exhibit-manager/pull/4), основана на выпущенном Word workflow, синхронизирована с main. PR #2 объединён в main; код main совпадает с v0.2.0, резервное копирование остаётся в PR #4.
- `project-backup-and-restore`: реализация и автоматические проверки завершены (16/16 задач), базовые спецификации синхронизированы; пользователь подтвердил ручной сценарий копии Word Windows 25 сентября после исправления OOXML. Архив включает исходники/редакции, настройки/решения, переводы и неизменяемую историю. Проверка перед записью, атомарное восстановление, явная копия при конфликте, ограниченные источником привязки Word, защита будущих преобразований схемы. Схема проекта остаётся 1.
- Черновик v0.3.0 удалён из GitHub Releases по запросу пользователя 24 сентября. 25 сентября пользователь подтвердил ручные проверки исправленной тестовой версии: условие ожидания выполнено. Перед выпуском пересобрать и проверить пакеты с исправлением OOXML; прежние ZIP его не содержат. Код и локальные пакеты из `768d8bf` сохранены. Публичный v0.2.0 не изменён.
- Следующие согласованные этапы: `self-service-onboarding`, затем `case-material-library`. Их код ещё не реализован. Архивирование завершённого change пока не выполнялось; перед ним сверить sync и ссылки ROADMAP.

## Проверки и границы

- Финальный CI `35994695642`: Windows x64 / Mac Intel / Apple Silicon, 186 Python + 11 JS, сборка/запуск/перенос пакетов и взаимный обмен архивами. На обоих Mac — реальный LibreOffice, новая редакция и новый ZIP после восстановления; исходники/старая история неизменны.
- Локально настоящий Microsoft Word Windows: восстановленный проект принимает новую редакцию и собирает новый DOCX/PDF с относительными ссылками. Это не тест Office.js.
- Браузер Chromium/Playwright Windows: 1440×1000 и 900×900, сохранение → отдельное хранилище → восстановление → новый DOCX → новый ZIP; также конфликт, отмена и повреждённый архив. Нет ошибок приложения; намеренно повреждённый ZIP даёт ожидаемый 400. Browser plugin отсутствовал.
- Нагрузка на `768d8bf`: 537 МБ, создание 18,08 с, восстановление 9,48 с, пик выделений Python 4,13 МиБ; хеши сохранены. Подробности и проверяемые сценарии — в tasks change.
- Пользователь подтвердил прежнюю ручную проверку панели Word. Не просить повторять её. Коллеги сообщают замечания по мере поступления; формальный ответ не нужен, молчание не считается тестом.
- Пользователь 25 сентября подтвердил: «всё ок по ручным проверкам» после установки исправления OOXML. Ручной сценарий восстановления копии и применения обновления названия через настоящую панель Word Windows пройден; не запрашивать его повторно без новой причины. Это отдельное подтверждение пользователя, не агентский тест и не проверка Office.js на Mac.

- Исправление 25 сентября: повторное чтение неизменённого диапазона настоящим Word меняет служебные rsid. Вместо сравнения строк OOXML используется значимый снимок с сохранением защиты текста, адресов и оформления. Пройдены 17 JS, 45 Python Word/backup, web build, OpenSpec strict 13/13. Headless Chromium + реальные фрагменты Word + работающий API подтвердили подготовку замены с сохранением начертания и адресом копии. Office.js в этой автоматической проверке подменён; живая панель отдельно подтверждена пользователем.
- Фоновое обновление установлено в `output/manual-v030-20260925/BLExhibitManager-0.3.0-windows-x64/_internal/web/dist/word/`: `office.js`, `ooxml-snapshot.js`. HTTPS 8778 отдаёт исправление с `Cache-Control: no-store`. После обновления пользователь подтвердил ручные проверки; агент не перезапускал приложение и не менял данные. Старый JS — `pane-before-snapshot-fix/`, локальные OOXML и headless-скрипт — в той же внешней папке проверки. Не коммитить эти материалы.

## Локальные материалы

- Для локальной dev-панели рабочий способ запуска — `Open-Word-test.cmd` (официальный sideload с учебным DOCX). Причина прежнего отказа при обычном открытии не установлена; полный перезапуск Word его не устранял. Не возвращаться к опровергнутому объяснению про незакрытый Word. Ручной сценарий в работающей панели теперь подтверждён.
- Пользователь попросил не мешать работе за компьютером: до нового явного разрешения не переключать окна, не запускать Word и не использовать мышь/клавиатуру. Продолжать только фоновую работу; ручное подтверждение пользователя уже получено.

- `output/release-v030/packages/` — прежние пакеты и SHA-256 из `768d8bf`, без исправления от 25 сентября. Перед выпуском пересобрать; не публиковать их как исправленные. `output/release-v030/release-notes.md` — прежний черновик. Assets удалённого релиза недоступны.
- `%TEMP%/bl-backup-qa/` — искусственный браузерный сценарий, скриншоты, метрики и ZIP; `output/backup-office/` — проверка с Word. Не коммитить артефакты.
- Текущая ручная среда: `output/manual-v030-20260925/Start-test.cmd`, отдельная копия учебных данных в `data/`, HTTP 8765 / HTTPS 8778. DOCX для пользователя — `Проверка восстановления.docx` в той же папке; проект «Word 365 — live test». Прежний 8793 сейчас не запущен.
- Прежние материалы: `output/release-v020/`, `output/word-live-office/`, `output/integrated-*`. `%TEMP%/bl-image-loss-review/` содержит частные материалы; никогда не публиковать.
