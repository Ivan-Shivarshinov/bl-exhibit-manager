# Текущее состояние работы

Обновлено 24 сентября 2026. Требования и задачи — в OpenSpec; сверять с Git.

## Контекст

- Ветка `codex/word-workflow`, PR #2. rc.5 и документация main объединены; main отдельно не обновлялся этим выпуском.
- Опубликован [v0.2.0](https://github.com/Ivan-Shivarshinov/bl-exhibit-manager/releases/tag/v0.2.0), исходный коммит `1b92f3e`. Обычный релиз по запросу пользователя; панель Word явно экспериментальная и необязательная. Прежний dev-черновик заменён, в Assets только три приложения и три SHA-256.
- Новые редакции сохраняют однозначные решения, включая «Без файла», исключения и ручные ссылки. Прежние ZIP неизменяемы; история результатов не заменяет резервную копию проекта.
- Основной DOCX/PDF-процесс не требует панели или сертификата. Обновление ссылок панели сохраняет свойства обозначения/названия и отклоняет смешанное оформление внутри одной части до записи.

## Проверки

- Финальный CI `35981655982`, коммит `1b92f3e`: Windows x64, Mac Intel и Apple Silicon — тесты Python/JavaScript, сборка, запуск, остановка, перенос пакета, редакции и история. На обоих Mac — реальная конвертация LibreOffice, изображения/прозрачная искусственная подпись, чёрное оглавление и относительные ссылки. Состав и SHA-256 опубликованных пакетов сверены.
- До изменения номера версии: настоящий Word Windows подтвердил полный HTTP-путь без панели, две редакции, ручные решения, относительные цели DOCX/PDF и неизменность прежнего ZIP/готовых PDF. Обмен OOXML через COM сохранил жирное обозначение и курсив названия; это не тест кнопки Office.js.
- Браузер Chromium/Playwright на Windows, ширина 1440/900: отчёт редакции, настройки панели, две версии истории и скачивание старого ZIP — без ошибок.
- Новые кнопки через живой Office.js не проверены: средство управления окнами не запускалось (`failed to write kernel assets`). Прежняя проверка основных кнопок — Microsoft 365 Windows 16.0.17928.20148, 21 сентября. Живой Office.js на Mac остаётся открытым.
- OpenSpec: строгая проверка всех восьми объектов. Публикация не закрывает оставшуюся приёмку.

## Продолжение

- Завершать активные `consolidate-release-line`, `optional-word-authoring`, `submission-revisions-and-history`; не архивировать до выполнения оставшихся критериев.
- Открыты расширенная проверка подготовки/перевода (задача 2.2), короткое название, запрет записи при tracking, сохранение курсива и отказ при смешанном оформлении через живую панель, переходы из перемещённого комплекта; Mac отдельно. Шаги — WORD-WORKFLOW-REVIEW.md.
- Не просить повторять подтверждённые тесты без новой причины. Не начинать будущие идеи ROADMAP автоматически.

## Материалы вне Git

- `output/release-v020/` — приложения v0.2.0, контрольные суммы, текст релиза и проверка пакетов; не коммитить артефакты.
- `output/stage-packages/`, `output/integrated-packages/` — предыдущие пакеты/отчёты; `output/integrated-*`, `output/word-rewrite-native/` — искусственные проверки.
- `output/word-live-office/` — прежний живой тест панели (8777/8778). `%TEMP%/bl-integrated-ui-20260923/` — изолированный проект HTTP 8781. Перед использованием проверить процессы.
- `%TEMP%/bl-image-loss-review/` — частный разбор изображений, не публиковать; потери отсутствующей уже во входном DOCX подписи приложением не доказано.
