# Текущее состояние работы

Обновлено 23 сентября 2026. Требования и задачи — в OpenSpec; сверять с Git.

## Контекст

- Ветка `codex/word-workflow`, PR #2. Панель Word строго опциональна. Основной DOCX/PDF-процесс не требует панели или сертификата.
- rc.5 объединён merge-коммитом `87c1f61`, документация main — `03c7663`. Опубликованный пилот остаётся `v0.1.3-rc.5`; новый этап ещё не опубликован.
- Доработана совместимость редакций с «Без файла», исключениями и ручными ссылками: перенос только при однозначном неизменном абзаце; новая редакция требует подтверждения. Каталог панели учитывает готовые PDF без штампов.
- Обновление названий через панель сохраняет свойства обозначения и названия из исходного XML. Смешанное оформление внутри одной части отклоняется до записи; повторно проверяются текст, XML и режим исправлений. Не удалять неподдерживаемые элементы Word.
- История ZIP неизменяема; это история результатов, не резервная копия проекта.

## Проверки

- 166 Python / 10 JavaScript тестов и Vite build прошли на Windows. OpenSpec: 8 объектов прошли строгую проверку.
- Настоящий Microsoft Word Windows: после интеграции прошли DOCX/PDF-ссылки в подпапках, чёрный цвет, жирное обозначение и обычный/курсивный текст названия, сохранение веб-ссылок и таймкодов.
- `scripts/verify_revisions_office.py`: полный HTTP-путь без панели, готовый PDF, ручные решения, загрузка второй редакции, повторный анализ, новые папки и два ZIP. Прежний ZIP и готовые PDF побайтово сохранены; цели DOCX/PDF проверены.
- Исходный XML настоящего Word обработан новым обновлением и вставлен обратно через COM: название изменилось, жирное обозначение, курсив и locator сохранились. Это проверка документов, не кнопки Office.js.
- Средство управления окнами не запускается (`failed to write kernel assets`). Новую кнопку обновления, короткую форму и режим исправлений через живой Office.js в этом проходе не проверяли. Прежняя проверка основных кнопок: Microsoft 365 Windows 16.0.17928.20148, 21 сентября.
- Сборки и конвертация на Mac проверяются отдельным CI; живой Office.js на Mac остаётся открытым. Не подменять эти проверки Windows или прежним подтверждением Mac M1/Acrobat.

## Продолжение

- Завершать `consolidate-release-line`, `optional-word-authoring`, `submission-revisions-and-history`. Выполненные части отмечены в tasks.md; изменения не архивировать до приёмки.
- Проверить пакеты CI трёх платформ, LibreOffice (изображения/оглавление/новые редакции), запуск и перенос. Затем подготовить проверочный релиз с кратким описанием и реальными ссылками; не выдавать его за стабильную версию.
- Живая приёмка панели: короткое название, запрет записи при tracking, сохранение курсива и отказ при смешанном оформлении, переходы из перемещённого комплекта; Mac отдельно. Инструкция в WORD-WORKFLOW-REVIEW.md.
- Не начинать будущие идеи ROADMAP автоматически. Не включать рабочие материалы в Git или релиз.

## Материалы вне Git

- `output/integration-tests.log`, `output/integrated-feedback/`, `output/integrated-reference-options/`, `output/integrated-revisions/` — текущие искусственные проверки.
- `output/word-rewrite-native/` — искусственный обмен XML с Word; `Source2.docx` и `Updated.docx`.
- `output/word-live-office/` — прежний живой тест панели и его отдельные данные (адреса были 8777/8778; процесс проверять перед использованием).
- `output/openspec-qa-location.json` — прежняя проверка независимого запуска при сломанном HTTPS.
- `%TEMP%/bl-image-loss-review/` — частный разбор изображений, не публиковать. Подписи не было уже в переданном DOCX; её потеря приложением не доказана.

Не просить повторять подтверждённые ручные проверки без изменения кода. Для управления окнами согласовать время, если пользователь работает за компьютером.
